# Digital Signatures API

## Setup

1. Run `pnpm install`.
2. Create and `.env` based on the `.env.example`.
3. Run `npx drizzle-kit generate && pnpm tsx src/database/migration.ts`
4. Run `pnpm dev`.
5. Open `http://localhost:3000`
